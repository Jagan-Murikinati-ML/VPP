heading :- Contains filtering and "Export to CSV" , functionality are not working on the Resources page.

description :- On the Resources page, the CONTAINS filter is not functioning as expected for the Program Name column.

When entering a value such as "z" in the Program Name filter, even though no records contain the entered character, the application still displays all site records instead of returning no results.

Expected Result:

Only records matching the entered filter value should be displayed.
If no matching records exist, the grid should display no records.

One more observation :- Ascending and descending sorting is not working properly for columns


